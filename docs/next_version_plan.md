# 下一版处理清单：精确解禁、技术评分落地、字段映射

本文件用于记录下一版要继续处理的事项，避免把开发计划散落在聊天记录里。

## 1. iFind 字段映射自动化

目标：不用再消耗 iFind API 额度去查询 `columns()`，优先用旧的 iFind 导出 CSV/Excel 中文表头反推字段。

已加入本包的准备工作：

- `scripts/build_ifind_field_mapping_from_exports.py`
- `build_ifind_field_mapping_offline.bat`
- 输出：`config/ifind_field_mapping_auto.csv`
- 输出：`deploy_data/ifind_field_mapping_auto.csv`

下一步要做：

- 将 `ifind_field_mapping_auto.csv` 接入日度更新主流程。
- 对 `p05310_f001`、`p03764_f001`、`p03412_f001`、`p03414_f001` 等字段建立稳定映射。
- 对低置信度字段进入“人工复核池”，不直接参与关键评分。

## 2. 精确解禁事件标准化

目标：解禁数据以 iFind 精确解禁明细为主，不再以规则估算作为主数据。

下一步要生成：

- `deploy_data/accurate_lockup_events.csv`
- `deploy_data/lockup_pressure_scored.csv`
- `deploy_data/lockup_event_study.csv`

核心字段：

- 股票代码
- 公司名称
- 解禁日期
- 解禁类型
- 解禁股东
- 解禁股数
- 解禁市值
- 占总股本比例
- 占流通股比例
- 解禁市值 / 20日平均成交额
- 股价较发行价涨幅
- 解禁前技术状态
- 解禁风险评级

## 3. 解禁影响规律模型

目标：不再“一有解禁就统一给风险评级”，而是按历史规律和条件分层评级。

事件研究窗口：

- T-60 至 T-1
- T-20 至 T-1
- T-5 至 T+5
- T+1 至 T+20
- T+1 至 T+60

分组维度：

- 解禁规模 / 20日成交额
- 股价浮盈程度
- 解禁类型：基石、控股股东、Pre-IPO、员工激励等
- 解禁前技术状态
- 市场环境和行业环境

## 4. 专业技术评分真正纳入二级交易评分

本包已包含技术指标计算脚本，但下一版要把技术评分真正接入：

- 决策总览的已上市公司二级评分
- 二级市场交易状态机
- 今日决策清单
- 单票投资备忘录
- 回测与有效性验证

技术评分模块：

- MA5 / MA10 / MA20 / MA60 / MA120
- MACD
- RSI14
- BOLL
- ATR14
- 成交额放大倍数
- IPO锚点：发行价、首日高低点、首日VWAP
- 技术状态：趋势确认、深V确认、等待回踩、破位弱势、放量滞涨、高位回撤

## 5. 评分科学性验证

下一版要新增或增强：

- A/B/C/D评级分布校准
- 高分组 vs 低分组收益对比
- 破发识别能力
- 因子有效性诊断
- 分年份、分市场环境稳定性验证
- 技术信号有效性验证
- 解禁事件影响验证

## 6. 低额度日度更新增强

当前策略：

- 静态表近端窗口
- 行情增量
- 快照只拉 2024+ IPO 股票池
- 失败保留旧数据

下一步增强：

- 断点续跑
- 分数据源单独运行
- 本地缓存差异检测
- 失败源次日补跑
- 更新后自动生成“数据可信度报告”
